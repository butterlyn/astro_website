Read specs/intent.md, specs/spec.md and SETUP.md.
Build a throwaway placeholder site on a new branch to prove the pipeline end to end. Do not touch main.
Allowed for this run only: placeholder content, no plan.md, noindex on every page, deploy from the branch via GitHub Actions to a preview Worker with a non-production name.
Everything else per the spec (section 3, R-07, R-08).
Extend the existing package.json, pnpm-lock.yaml and pnpm-workspace.yaml rather than replacing them. Preserve the Node, pnpm, TypeScript and Astro checker pins documented in SETUP.md. Install the remaining compatible dependencies with exact versions and review install-script permissions.
Keep the public repository on GitHub Free and preserve the enforced protection on main. Use the deployment safeguards in SETUP.md: validation must succeed before deployment, deploy the same tested artifact, limit deployment to this branch, and give Cloudflare secrets only to trusted deployment/cleanup steps. Do not add a paid plan requirement or change repository visibility.
Run pnpm check:setup with the other validation checks. Run pnpm preflight:cloudflare in the trusted deployment job before deploying; it checks account access and the workers.dev subdomain without changing Cloudflare resources. A preflight failure must stop deployment and report the concrete account-side fix.
Implement cleanup through a dedicated marker commit on the throwaway branch: an ordinary branch push runs build and deployment, while a push that adds `.cleanup-preview` runs only the preview Worker deletion job. Do not use `workflow_dispatch`, because the dummy workflow is not added to main.
Serialize deployment and cleanup for the same Worker so an older deployment cannot recreate it after deletion. Before the successful deployment push, push a deliberate validation failure and verify that deployment is skipped; then fix it and run the full pipeline. Do not weaken or remove the required checks to make this demonstration pass.
Push, watch the Actions run with gh, verify the live URL with curl, then report the URL and anything skipped or failed, and stop.
When I later say "clean up": add and push `.cleanup-preview`, watch the cleanup job, confirm the Worker is gone, and delete the branch locally and on GitHub.
