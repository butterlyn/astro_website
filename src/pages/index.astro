---
import { getEntry } from "astro:content";
import HomePage from "../layouts/HomePage.astro";

const page = await getEntry("homepage", "home");
if (!page)
  throw new Error("The homepage requires src/content/homepage/home.md.");
---

<HomePage page={page} />
